rm -rf .git

建立 .gitignore

Library/
Temp/
Obj/
Build/
Logs/
UserSettings/

git init

git add .

git commit -m "initial commit"


git remote add origin git@github.com:你的帳號/專案.git


git branch -M main

git push -u origin main


rm -rf .git  ← 重建（最乾淨）